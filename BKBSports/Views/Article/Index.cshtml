@model IEnumerable<BKBSports.Models.ArticleModel>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.articleCreateDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.articleUpdateTimestamp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.authorId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.layout)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.approvalIndicator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.articleImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.articleContent)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.articleCreateDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.articleUpdateTimestamp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.authorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.layout)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.approvalIndicator)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.articleImage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.articleContent)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.articleId }) |
                @Html.ActionLink("Details", "Details", new { id=item.articleId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.articleId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
